{
  "name": "mobile",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mobile",
  "projectType": "application",
  "tags": ["scope:mobile-app"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "apps/mobile"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint .",
        "cwd": "apps/mobile"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --reporter=default",
        "cwd": "apps/mobile"
      }
    },
    "test:watch": {
      "continuous": true,
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest",
        "cwd": "apps/mobile"
      }
    },
    "serve": {
      "continuous": true,
      "executor": "nx:run-commands",
      "options": {
        "command": "expo start",
        "cwd": "apps/mobile"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "expo export -p web",
        "cwd": "apps/mobile"
      }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/playwright-report", "{projectRoot}/playwright-test-results"],
      "options": {
        "command": "playwright test",
        "cwd": "apps/mobile"
      }
    },
    "runtime-smoke": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/playwright-report", "{projectRoot}/playwright-test-results"],
      "dependsOn": ["build"],
      "options": {
        "command": "pnpm exec playwright test -c playwright.runtime-smoke.config.ts",
        "cwd": "apps/mobile"
      }
    },
    "e2e-real-backend": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/playwright-report", "{projectRoot}/playwright-test-results"],
      "dependsOn": ["build", { "projects": ["server"], "target": "build" }],
      "options": {
        "command": "pnpm exec tsx e2e/_support/real-backend-runner.ts",
        "cwd": "apps/mobile"
      }
    }
  }
}
