// PHASE 1 PLACEHOLDER — bottom tab bar icons + ordering wired in T037.
import { Tabs } from 'expo-router';

export default function TabsLayout() {
  return <Tabs screenOptions={{ headerShown: false }} />;
}
