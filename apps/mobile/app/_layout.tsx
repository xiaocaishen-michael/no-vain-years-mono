// PHASE 1 PLACEHOLDER — route shell only; AuthGate + providers wired in T034.
import { Stack } from 'expo-router';

export default function RootLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
