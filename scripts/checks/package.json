{
  "name": "@nvy/checks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Mono-local governance check scripts (ADR index / env sync / server bounded-context moat). Preset-installed checkers (check-adr-frontmatters / check-spec-frontmatters) stay at scripts/ root per their preset contract.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "ts-morph": "^28.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.9.2",
    "vitest": "^4.1.6"
  }
}
